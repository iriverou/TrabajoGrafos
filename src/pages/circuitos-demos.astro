---
import Layout from '../components/layout.astro';
import CircuitDemo from '../components/circuitdemo.astro';
import { CIRCUIT_PATTERNS, CIRCUIT_INFO } from '../scripts/circuit-patterns.js';
---

<Layout title="Circuitos Básicos - Wireworld">
    <h2>Circuitos Básicos de Wireworld</h2>
    <p>Explora la simulación interactiva del conductor simple en Wireworld. Usa los controles para iniciar, pausar o reiniciar la demo y observa el comportamiento básico de los electrones.</p>

    <div class="instructions">
        <h3>Cómo usar la demo:</h3>
        <ol>
            <li><strong>Haz clic en "Iniciar"</strong> para comenzar la simulación</li>
            <li><strong>Observa</strong> cómo fluyen los electrones por el conductor</li>
            <li><strong>Usa "Reiniciar"</strong> para volver al estado inicial</li>
            <li><strong>Experimenta</strong> pausando y reanudando la simulación</li>
        </ol>
    </div>

    <div class="demos-section">
        <h3>Selección de demos</h3>
        <CircuitDemo 
            title={CIRCUIT_INFO.SIMPLE_WIRE.title}
            description={CIRCUIT_INFO.SIMPLE_WIRE.description}
            pattern={CIRCUIT_PATTERNS.SIMPLE_WIRE}
            width={192}
            height={120}
        />
    </div>
</Layout>

<style>
    :where(.instructions) {
        background-color: var(--color-info-bg);
        padding: 20px;
        border-radius: 8px;
        border-left: 4px solid var(--color-border);
        margin: 20px 0 30px 0;
    }
    
    .instructions ol {
        margin: 10px 0 0 20px;
    }
    
    .instructions li {
        margin: 8px 0;
        line-height: 1.5;
    }

    .demos-section {
        margin: 30px 0;
    }

    .demos-section h3 {
        margin-top: 0;
        color: var(--color-text); /* Título azul */
    }
    .info-note {
        background-color: var(--color-info-bg); /* Fondo info-note */
        padding: 20px;
        border-radius: 8px;
        border-left: 4px solid var(--color-border); /* Borde info-note */
        margin-top: 30px;
    }

    .info-note h4 {
        margin: 0 0 15px 0;
        color: var(--color-text-blue); /* Título azul */
    }

    .info-note ul {
        margin: 10px 0;
        padding-left: 20px;
    }

    .info-note li {
        margin: 8px 0;
        display: flex;
        align-items: center;
        gap: 10px;
    }

    .color-sample {
        width: 20px;
        height: 20px;
        border-radius: 3px;
        border: 2px solid var(--color-main-border); /* Borde muestra color */
        display: inline-block;
        flex-shrink: 0;
    }

    .color-sample.empty { background-color: var(--color-empty); /* Estado vacío (Wireworld) */ }
    .color-sample.conductor { background-color: var(--color-conductor); /* Estado conductor (Wireworld) */ }
    .color-sample.electron-head { background-color: var(--color-electron-head); /* Estado electron-head (Wireworld) */ }
    .color-sample.electron-tail { background-color: var(--color-electron-tail); /* Estado electron-tail (Wireworld, rojo consistente) */ }
</style>